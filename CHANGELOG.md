# 更新日志

本文件记录本项目所有值得注意的变更。

格式参考 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/)，
版本号遵循[语义化版本](https://semver.org/lang/zh-CN/)。

## [未发布]

### 修复

- 缓存时间显示改为按**当前本地时区**换算（此前直接使用写入时的偏移，
  把配置目录拷到别的时区后"上次更新"会与本地时钟不一致）。
  该问题由 CI 在 UTC 时区的运行器上首次执行时暴露

### 工程

- 新增 GitHub Actions 工作流（Windows / Python 3.12）：push 与 PR 自动运行全部 407 项测试；
  运行器具备 Tk 桌面会话时额外执行一次应用自检并归档报告
- 拆出 `requirements-dev.txt`（运行时依赖 + pytest）
- 测试断言不再写死时区，并新增两条与时区无关的不变量测试

### 文档

- README 增加安装包下载入口、CI 与许可证徽标
- 移除文档中的机器专属绝对路径，改为仓库内相对路径
- 新增本文件

## [1.0.0] - 2026-09-19

首个发布版本，对应 [Release v1.0.0](https://github.com/collinszheng/SteamGamePicker/releases/tag/v1.0.0)。

### 新增

- **身份解析**：支持 17 位 SteamID64、`/profiles/` 资料 URL、`/id/` 自定义名、裸自定义名四种写法；
  自定义名通过 `ResolveVanityURL` 解析，失败时给出可执行提示
- **游戏库**：`GetOwnedGames` 拉取（后台线程执行，界面不卡顿），过滤 `appid<=0` 与无名条目；
  本地库快照缓存，**启动秒开、断网也能抽签**；详情与封面缓存默认 7 天
- **范围筛选**：「全部参与」/「从未玩过」/「玩得很少」（后两者可**同时勾选**，取并集），
  列表中还可逐条勾选排除；搜索支持 300ms 防抖与多关键词，列表可按名称或游玩时间排序
- **抽签**：1.8 秒滚动减速动画（50ms × 24 + 55/70/90/110/130/145ms），
  结果从当前勾选集合中均匀随机产生；池中仅 1 款时跳过滚动直接定格
- **结果展示**：封面、简介、类型、发行日期、Metacritic 评分、售价（只显示未打折原价）；
  免费游戏显示「免费」，缺失字段统一显示 `—`
- 设置窗口与首次运行引导；快捷键：`Enter` 加载、`空格` 抽签、`F5` 刷新、`Ctrl+,` 设置

### 界面

- 改为 **Steam 官方深色风格**：`#171a21` 顶栏 / `#1b2838` 底色 / `#2a475e` 面板 /
  `#66c0f4` 强调蓝 / `#c6d4df` 正文，主按钮用商店绿 `#4c6b22`；
  `ttk` 主题切换到 `clam`（Windows 原生主题不支持自定义背景色）
- 列表加斑马纹、被排除的游戏灰显；列表区与结果区卡片化；顶栏加 Steam 蓝强调线
- 窄窗口自适应：窗口高度 < 520 时自动收起列表区（**不覆盖**用户保存的展开偏好）；
  未抽签时不占封面位，把高度让给游戏列表

### 修复

- **证书信任回退**：企业网络中间层或杀软 HTTPS 扫描导致 `SSLCertVerificationError` 时，
  自动切换到操作系统信任库重试一次（**任何情况下都不关闭证书校验**），
  并给出专门中文提示，不再笼统报「网络连接失败」
- 抽签定格结果被「请输入 Steam ID…」占位文案覆盖
- 加载中「刷新」按钮未按状态机禁用；详情 [重试] 按钮可被连点重复提交
- 缓存清理的「快照豁免」破坏 200 MB 上限保证；静默卸载时确认框卡住自动化部署

### 工程

- **405 项 pytest**：纯逻辑、网络契约（脱机 Steam 响应样本）、界面交互、性能预算、
  异常矩阵、主题对比度（WCAG AA）、逐控件越界检测
- 打包：PyInstaller onefile（实测冷启动约 2.0 秒）+ Inno Setup 安装包
  （非管理员可安装、卸载默认保留配置、支持静默安装）
- 配置原子写与损坏自动备份恢复；日志轮转并脱敏 API Key

### 文档

- [产品需求文档](docs/PRD.md) v1.5（含 D1–D10 决策记录与 AC-01 ~ AC-52 验收标准）
- [开发计划](docs/DEV_PLAN.md)、[验收记录](docs/ACCEPTANCE.md)（逐条结论、证据与偏差说明）
- 界面截图：`docs/evidence/ui-steam-theme-loaded.png`、`docs/evidence/ui-steam-theme-result.png`

### 已知限制

- 必须自备 Steam Web API Key（Steam 不允许第三方读取未授权数据）
- 仅支持 Windows 10 / 11 64 位
- 不判断游戏是否已安装，也不提供启动游戏功能
- 验收记录中「非公开账号 / 空库账号 / 无效 Key」三类真实账号场景，
  以及干净 Windows 10 机器上的安装验证，尚待在对应环境补测
